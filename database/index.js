import users from "./users.json" assert { type: "json" };
import messages from "./messages.json" assert { type: "json" };
import privateMessages from "./privateMessages.json" assert { type: "json" };

export default {
	users,
	messages,
	privateMessages,
};
